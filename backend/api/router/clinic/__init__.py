"""
Clinic-side routers (frontend `service: "backend"` URLs).

These match the exact URL patterns the React frontend already calls — see
AswinAI_TZ-main/src/features/*/services/*Api.* for the contract.
"""
